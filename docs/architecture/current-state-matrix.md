# Patchwork current-state matrix

Updated: 2026-08-21

This is the authoritative description of what the Patchwork runtime does today. It distinguishes tested contracts and in-memory models from durable or externally integrated behavior. The buyer-ready sequence and exit gates live in [`2026-07-28-buyer-ready-web-completion-roadmap.md`](../superpowers/plans/2026-07-28-buyer-ready-web-completion-roadmap.md).

The target for future responsive-web feature work is defined separately in
[`buyer-ready-web-charter.md`](../product/buyer-ready-web-charter.md). The
charter is not current-state evidence; this matrix must continue to describe
only behavior that is implemented and verified.

## Maturity labels

| Label | Meaning |
| --- | --- |
| `contract-only` | Types, validators, domain rules, fixtures, or tests exist, but there is no complete runtime path. |
| `fixture-runtime` | A runnable path exists, but its primary state or external behavior is simulated or process-local. |
| `partially-persistent` | A runnable path uses durable storage for part of its behavior, while material state or integration remains simulated or volatile. |
| `externally-integrated` | The runtime talks to its intended external system and has integration coverage, but production operations have not been demonstrated. |
| `production-ready` | External integration, durability, security, deployment, recovery, monitoring, and operational ownership are all demonstrated. |

No subsystem is currently `production-ready`.

Every subsystem included by the buyer-ready web charter has a
`partially-persistent` or `externally-integrated` runtime path. Rows that remain
`fixture-runtime` or `contract-only` below are explicitly excluded expansion
work such as native mobile, multi-region, offline sync, reputation, and
external connectors.

## Alpha-critical subsystems

| Subsystem | Maturity | Runtime evidence | Persistence boundary | Test evidence | Material gap |
| --- | --- | --- | --- | --- | --- |
| AT lexicons and validation | `externally-integrated` | `packages/at-lexicons/src/lexicons.ts`, `validators.ts`, and five JSON lexicons load and validate local records. The official client adapters encode aid and directory coordinates as AT-compatible integer microdegrees/metres at the PDS boundary; shared ingestion decodes that wire representation before strict validation and normalization. | JSON schemas and fixtures are repository files; disposable live-PDS records have been created, read, updated, and deleted. | Lexicon, aid/directory client, and firehose tests cover strict public shapes, coordinate round trips, privacy precision, and live-wire decoding; redacted two-account PDS evidence covers the aid family. | Lexicon publication/registration, live directory compatibility evidence, compatibility fixtures for every remaining record family, and production operations remain incomplete. |
| Identity and authentication | `externally-integrated` | Non-test API startup constructs the official Node OAuth client. Login can return an opaque authorization URL to the SPA. Callback success sets HttpOnly session plus readable CSRF cookies, strips sensitive query fields, and routes through a same-origin callback page that must restore the new cookie-backed session before entering the requested route; failure exposes only a stable error code and retry path. Managed signup validates handles and returned DIDs, rejects browser identity/privilege fields, applies signup rate limits, maps upstream failures without credential leakage, and then routes the new user through the same OAuth/session flow. `AuthProvider` restores, refreshes, expires, and revokes the cookie-backed session; authenticated shell actions derive the DID from it. | The PDS owns managed account credentials and public identity. OAuth state and SDK session payloads use AES-256-GCM PostgreSQL stores; opaque browser tokens are stored only as hashes. Browser code receives only DID and expiry and contains no access/refresh-token model. | OAuth adapter/error tests, API auth/store persistence tests, provider/API/signup flow tests, production-bundle callback restoration/retry cases, and two earlier controlled live browser callbacks cover login and recovery semantics. Signup tests cover forged fields, malformed upstream identity, throttling, and safe errors. | A fresh credentialed live callback repetition, managed-signup exercise, PDS recovery-email operations, protected staging repetition, credential rotation, and production operations remain incomplete. |
| Aid-post record operations | `externally-integrated` | Authenticated `/at/aid-posts` commands restore the caller’s OAuth session and use the official `Agent` create/get/put/delete APIs. Ownership comes from the restored DID; updates, close, and delete require CID compare-and-swap. A controlled two-account browser journey verified create, live discovery, lifecycle update, report/block, close, and delete through the complete Patchwork path. | The PDS is the public-record authority; PostgreSQL persists OAuth state, projections, tombstones, and lifecycle reconciliation state. | Command-service tests; official-agent transport tests including wire encoding, cross-DID rejection, invalid records, stale revisions, and PDS errors; redacted live-PDS protocol evidence; automatic projection/lifecycle integration tests; and the real Chromium journey. | Protected automated disposable-account provisioning/cleanup and production operations remain incomplete. |
| Directory record operations | `externally-integrated` | Authenticated `/at/directory-resources` create/get/update/delete commands restore the caller’s OAuth session and use the official `Agent` repository APIs. The restored DID enforces ownership; updates and deletes require CID compare-and-swap. The API forces new records to `unverified` and preserves server-read verification and creation state during owner edits. A controlled browser exercise published, updated, discovered, and deleted a real directory record through the home PDS and local Jetstream. | The PDS is the public-record authority. PostgreSQL retains opaque browser/OAuth state and rebuildable directory projections; command idempotency responses survive restart. | Directory client and command tests cover integer wire encoding, strict records, privacy precision, deterministic create keys, cross-repository denial, compare-and-swap, and self-verification prevention. API-client/form tests cover typed CRUD and validation. Rendered Chromium and controlled live workflows cover create/read/edit/delete. | A trusted partner-verification administration workflow and production operations remain incomplete. |
| Live AT ingestion | `externally-integrated` | The production indexer runs Jetstream v2 in `v2-live` mode. It filters Patchwork collection events, resumes its PostgreSQL sequence checkpoint, writes normalized events, reconciles compatible aid-post lifecycle status/deletion, and only then acknowledges them. The v2 cutover retains the v1 projection and checkpoint in rollback storage. The NUC-local Jetstream consumed the home PDS repository stream before this cutover because the public relay did not carry custom records from that PDS. Controlled aid and directory lifecycles traversed that earlier path. | Cursor, CID/revision, hashed author identity, approximate geography, searchable fields, public record state, idempotency ledger, hashed deletion tombstones, bounded dead letters, heartbeat, private workflow state, and minimal reconciliation audits survive restart. V2 control state and rollback data are durable PostgreSQL state. | Local v2 source, bounded backfill, projection comparison, shadow-store, migration, and replay tests cover sequence checkpoints, control events, duplicate/stale events, and cutover boundaries. Earlier controlled browser exercises proved both live record families through the v1 path. | The v2 cutover has no fresh protected-staging or live-PDS evidence. Repeat the full projection comparison, replay, disconnect drill, and lifecycle journey before treating v2 as operationally verified. |
| Discovery map/feed/directory | `externally-integrated` | PostgreSQL mode constructs `PostgresProjectionQueryService`, which reloads durable aid-post and directory-resource projections and applies deterministic filters and ranking. Anonymous discovery remains public; authenticated discovery derives the viewer DID and bilaterally excludes blocked authors. Aid circles use a stable per-record display displacement after the minimum-1km quantization, progressively combine/split by zoom, and expose labeled filled/outline/high-contrast styles. Selecting a circle centers it and creates an explicit URL-backed area filter with browser Back/Forward, clear, and return-to-previous-area behavior. Directory results add an exact point only when separate moderator approval, matching organization verification, matching resource verification, and active stewardship are all current. Confidential, stale, revoked, forged, and unapproved exact addresses fail closed. | Production reads durable aid, directory, volunteer, verification, stewardship, and block state. Approximate public AT geography remains the normal location boundary; the rendered aid-circle center is deliberately displaced from the supplied approximate coordinate. Exact public-resource coordinates exist only in private PostgreSQL approval state and are never written to a user AT record. Circle-style preference is non-sensitive browser local storage. | PostgreSQL tests cover filters, stable pagination, block filtering, volunteer discovery, exact-address positive projection, and immediate removal after a verification gate is revoked. Component and production-bundle Chromium cases prove displacement, zoom aggregation, archive overzoom, labels, exact-place gating, explicit reversible filters, history restoration, and style persistence. | Protected staging repetition and production operations remain unavailable. |
| Minimal request lifecycle | `externally-integrated` | In PostgreSQL mode, transition, query, assignment, acceptance, decline, handoff, timeout, and confirmed-record deletion paths use `PostgresLifecycleRepository`; status, actor role, ordered timeline, assignment and handoff metadata, and audit records survive restart. Confirmed PDS or stream deletion removes the workflow and cascading fulfillment data while retaining a minimal replay-safe audit marker. PostgreSQL roles are resolved by authenticated DID on every request and default safely to `user`. Owners load private lifecycle state through an authenticated owner-or-moderator boundary; author commands synchronize private state outward through PDS compare-and-swap and expose retryable partial-failure UX; validated repository events synchronize compatible public status/deletion inward before cursor acknowledgement and record divergence without regressing private state. | PostgreSQL tests demonstrate restart survival, role resolution, authenticated HTTP elevation, private owner isolation, public-sync state and checkpoint idempotency, duplicate-command suppression, rollback, timeline reconstruction, deletion reconciliation, atomic assignment, timeout, handoff, stale-event exclusion, and failure redelivery. The controlled two-account browser journey proved live resolve, public close reconciliation, and deletion. | `aid-post-command-service.test.ts`; `lifecycle-service.test.ts`; `durable-core-services.test.ts`; `core-operational-state.test.ts`; `lifecycle-reconciler.postgres.test.ts`; HTTP/PostgreSQL and direct-service integration suites; controlled external Chromium journey. | Role provisioning is operator-only and requires an audited database session during alpha; protected staging repetition remains external. |
| Blocks and reports | `externally-integrated` | Authenticated browser controls call method-aware `POST /blocks` and `POST /reports` routes. Session-derived actors and idempotency protect PostgreSQL-backed mutations. Active blocks bilaterally filter authenticated discovery and immediately terminate direct or group chat authorization. Chat reports accept only authorized received messages and queue a body-free digest/count evidence record. Account deactivation deletes owned blocks and strips details from bounded retained blocks/reports involving the subject. | Private rows carry retention metadata; deletion soft-deletes blocks and clears private report details. Chat evidence retains no message body and expires after 30 days. | Browser report/block/chat cases, authenticated HTTP identity tests, durable PostgreSQL restart tests, repository tests, and the controlled helper session prove durable behavior and deactivation sanitization. | Formal policy approval, independent privacy/security review, and protected staging observation remain external. |
| Moderation queue and audit | `partially-persistent` | Production startup requires PostgreSQL and routes queue, pre-publication review, policy, appeal, and audit operations through durable stores. Schema, length, U.S.-area, minimum-precision, emergency-intent, prohibited-content, sensitive-data, embedded-file, and private-attachment scan checks run before public AT writes. Only an explicit accepted result proceeds; uncertain content is suspended with stable safe reasons and provider failure fails closed. The authenticated moderator console exposes filters, evidence-safe previews, one-action quarantine, appeals, audit history, verification links, and urgent channel delivery. | Content hashes, safe previews, decisions, urgent events, queue state, leases, policy state, audit history, idempotency, and retention deadlines survive restart; raw submission bodies do not enter review tables. Active cases never expire by age alone. | PostgreSQL tests prove accepted/rejected/quarantined paths, provider failure, attachment ownership/scan state, restart replay, urgent-event deduplication, retention, and raw-content absence. Unit/API tests prove publication is not invoked after a held or unavailable decision. Chromium proves moderator authorization, quarantine, appeal progression, audit display, and privacy-safe rendering. | Public exposure of the worker is prohibited. Formal privacy approval, policy review, and protected staging alert delivery remain external. |
| API transport and authorization | `partially-persistent` | The production surface is method-aware, authenticated, authorized, CSRF-protected, bounded, and free of URL mutations. Domain mutations require an `Idempotency-Key`; the PostgreSQL ledger serializes delivery and replay. A durable maintenance declaration for privacy, authorization, abuse, integrity, moderation backlog, monitoring, or backup failures blocks every new-submission and exact-location write before effects while `/status` and safe reads remain available. Resume requires `maintenance_mode:manage`, is audited, and cannot override an environment shutdown. | Sessions, roles, private state, pre-publication decisions, maintenance state/audit, and idempotency responses are PostgreSQL-backed; limiter state intentionally resets per process. New review and maintenance audit rows carry enforced retention deadlines and are pseudonymized on deactivation. | PostgreSQL and HTTP tests cover restart persistence, public status, invalid/unauthorized declarations, environment override, blocked route classification, audit retention, deactivation sanitization, and fail-closed publication. Browser tests prove read-only status messaging and posting shutdown. | Formal retention/privacy approval remains incomplete. Multi-replica rate limits eventually need a shared backend; no fixture fallback is permitted. |
| Web alpha journey | `externally-integrated` | The cookie-only authenticated shell includes durable volunteer, organization, verification, attachment, coordination, connection scheduling, groups, bounded text chat, notification, moderation, settings, export, and deactivation journeys. English and Spanish cover every production route; preference changes preserve URL/form state. The moderator route is capability-gated, and maintenance renders a global read-only notice. Two accounts can separately establish an authenticated encrypted peer channel for ephemeral exact location; chat is server-readable and never described as E2EE. | Public records use the owner PDS and durable PostgreSQL projections. Private workflows, schedules, groups, chat, and moderator state use PostgreSQL; private bytes use S3-compatible storage. Exact personal coordinates remain browser-memory/peer-channel only. Fixture data is limited to explicit fixture development mode. | Unit, HTTP/PostgreSQL, real MinIO/clamd, service-integration, key-parity/static-copy checks, and the 148-case Chromium matrix cover localization, scheduling, groups, chat, accessibility, privacy, reversible map interactions, maintenance, moderation, and honest trust disclosure. The clean final production-bundle run passed 147 cases with one credentialed external journey skipped. | Credentialed live repetition, professional translation review, independent accessibility/security/privacy review, protected staging alert receipt, and production operations remain incomplete. |
| Buyer-ready showcase data | `partially-persistent` | `db:seed:showcase` transactionally creates fixed fictional people, approximate aid/directory/volunteer projections, a resolved workflow, accepted offer, completed connection, outcome, fictional organization, attributed City of Chicago public-source reference, moderation case, and notification. Replay uses one advisory lock and refuses untagged reserved-key collisions. Discovery and moderator UI expose origin labels. | PostgreSQL stores immutable origin/provenance metadata and direct projection/moderation origin columns. Synthetic refresh deletes only rows already labeled for the same seed; visitor rows survive. Sourced metadata requires HTTPS provenance, retrieval/verification dates, and a non-participation flag. | A fresh-database PostgreSQL suite proves deterministic two-pass replay, visitor preservation, immutable origin, `.invalid` contact, ≥1 km service areas, official-source attribution, export labels, and refusal to deactivate showcase identities. | Public-source freshness still requires an operator to verify and version a future seed; the sourced reference is not a participation or service-availability claim. |

## Supporting and expansion subsystems

| Subsystem | Maturity | Runtime evidence | Persistence boundary | Test evidence | Material gap |
| --- | --- | --- | --- | --- | --- |
| Groups and chat | `partially-persistent` | Production API startup exposes authenticated/idempotent group and bounded-chat routes. Groups enforce owner/moderator/member capabilities, hashed seven-day single-use invitations, rooms, optional intersection-authorized request links, and immediate membership/role/block/moderation checks. Direct chat is bound to an active accepted connection; room chat is bound to current active membership. Messages are bounded to 2,000 characters and 20 active sends per minute, deduplicated by client UUID, cursor-paginated, receipted, redactable by the sender for 24 hours, and reportable without copying the body into moderation history. | Groups, memberships, rooms, invitation hashes, conversations, participant state, messages, receipts, audit metadata, and retention deadlines persist in PostgreSQL. Message text exists only in the message table, is retained up to 365 days, is omitted from list previews/notifications/audits, and is redacted on sender deactivation. | Fresh migration replay; PostgreSQL restart/capability/privacy/deactivation tests; authenticated HTTP identity/idempotency tests; static localization/body-absence checks; and responsive axe-checked Chromium group/chat journeys pass. Legacy `/chat/initiate` remains unreachable. | Protected staging repetition, independent security/privacy/accessibility/translation review, and production operations remain incomplete; the operational `NO-GO` remains. |
| Buyer-ready safety contracts | `partially-persistent` | Strict location, consent, origin, organization, verification-review, exact-public-address, private-attachment, pre-publication moderation, and maintenance contracts are enforced by the durable runtime. Precise personal publication remains rejected, approved exact public-resource addresses are separately gated and automatically withdrawn, and required safety checks fail closed. | Consent, origin metadata, organizations, verification, appeals, exact-address approvals, attachment jobs, moderation reviews, urgent events, and maintenance audit persist in PostgreSQL; private bytes use server-generated object keys and user-owned public records remain in the PDS. Personal peer coordinates, proofs, session identifiers, SDP, and ICE signals have no PostgreSQL or AT schema and are excluded from exports, notifications, analytics payloads, and backups. | Shared negative-contract, PostgreSQL, authenticated HTTP, real MinIO/clamd, static absence, API-client, and Chromium tests cover forged authority, confidential locations, unsafe submissions/files, provider failure, access loss, retry/restart/deletion/orphan paths, fresh consent, role/single-use enforcement, revoke/maintenance, browser clearing, and prohibited-payload absence. | Protected staging attachment/provider delivery and cross-network peer repetition remain external operational evidence. |
| Settings and privacy controls | `partially-persistent` | Production Settings exposes session-derived onboarding status plus authenticated preference reads/writes. Current-version consent records the accepted five-document set, timestamp, and required 18+ assertion; a material version change makes protected API and web actions fail closed until renewal. Preferences cover privacy, in-app/email/push selection, profile visibility, English/Spanish choice, approximate-or-hidden location, and no-permanent-address. `GET /account/export` includes these records. Explicit, idempotent `POST /account/deactivate` removes them with other Patchwork-owned state, revokes sessions, clears cookies, and retains only bounded safety/audit exceptions and a hash suppression receipt. | PostgreSQL stores versioned consent, current preferences, and a preference-change audit. Export excludes credentials, lookup hashes, internal payloads, private casework, and preference audit internals. Deactivation deletes consent, preferences, preference audit, projections/workflows/roles/owned blocks, and command metadata; retained attribution is bounded and pseudonymized. | Strict shared schemas, handler tests, PostgreSQL restart tests, account export/deactivation integration, API-client tests, and Chromium renewal/preference journeys prove stale-policy rejection, session-derived ownership, exact-location rejection, persistence, export, and deletion. | Patchwork cannot delete independently hosted AT-repository records. A credentialed live signup, formal privacy approval, and real staging/backup exercises remain external gates. |
| Volunteer profiles | `externally-integrated` | Authenticated official-client create/read/update/delete commands bind the owner to the restored AT session. Public discovery comes from cursor/replay/delete-aware PostgreSQL projections with bilateral block filtering. The web separates public capabilities, service area, no-permanent-address, and approximate location from private contact and matching fields. | The PDS owns the public profile; PostgreSQL stores rebuildable public projection state and the owner-only private profile. Export and deactivation cover both Patchwork-held boundaries. | Official-client, indexer, PostgreSQL restart, HTTP ownership, privacy, and authenticated Chromium publish/update/discover/delete tests pass. | A controlled live-PDS volunteer exercise, protected staging repetition, and production operations remain incomplete. |
| Verification and appeals | `partially-persistent` | Authenticated applicants upload private evidence, wait for clean scan status, and submit only clean evidence references for volunteer, organization, or stewarded-resource verification. Capability-gated moderators receive actor-bound preview controls and can quarantine, rescan, or delete evidence before approving, denying, renewing, revoking, or resolving appeals. Approvals expire after one year; an hourly scheduler expires them and revokes dependent exact addresses. Every transition is privately audited, and submission, decision, appeal, and expiry transitions atomically enqueue privacy-safe notifications. Reviewer evidence controls never appear for unauthorized accounts or public discovery. | Applications, private object-backed evidence metadata, scan attempts, decisions, appeals, exact-address requests, notification intents, and audit events persist in PostgreSQL and survive service restart. Exact coordinates are excluded from AT records, notification channels, and general logs. Export includes safe attachment and notification metadata without object keys, bodies, tokens, signed URLs, push endpoints, or subscription keys; deactivation deletes the records and objects and redacts retained audit attribution. | PostgreSQL and real MinIO/clamd tests prove restart, malware/uncertain quarantine, image transformation, signed reviewer access, annual expiry, renewal/revocation gates, appeal/uphold, atomic notification creation, ownership/status, public projection removal, export, and deactivation. Authenticated Chromium covers applicant upload and moderator evidence controls. | Protected staging repetition and production operations remain incomplete. |
| Attachments | `externally-integrated` | Production startup constructs a PostgreSQL-backed `AttachmentService` using private S3-compatible storage and a TCP clamd scanner. Authenticated routes authorize and complete bounded uploads, list safe metadata, issue actor-bound 60-second clean-content access, delete owned objects, and expose capability-gated moderator quarantine/rescan/delete. Images are decoded/re-encoded without metadata; PDFs are validated; originals and derivatives are scanned. | PostgreSQL stores safe attachment metadata, private server-generated object references, scan attempts, reviewer actions, retention, and durable deletion jobs. MinIO/S3 stores private original and transformed bytes. Subject/account/policy/retention/moderator deletion queues both variants; retries and orphan reconciliation repair drift. Exports exclude bodies, keys, tokens, and signed URLs. | Focused PostgreSQL tests cover clean/spoof/malware/uncertain outcomes, three-attempt retry exhaustion, restart, access roles, every deletion trigger, failed-deletion retry, and orphan repair. Real MinIO/clamd integration covers clean transform/access, custom-signature malware quarantine, service reconstruction, and physical object deletion. API-client, web-service, Compose, and Chromium tests cover posting, verification, moderator controls, and no-key boundaries. | Protected staging repetition, production malware-definition and credential operations, independent object backup/restore, capacity, and human incident-response evidence remain incomplete. |
| Offers and connections | `partially-persistent` | Authenticated production routes create offers and enforce requester/helper-only accept, decline, cancel, completion, and expiry transitions. Safety, deactivation, bilateral block, moderation, request-state, ownership, and active-profile conditions are rechecked at every transition. Pending offers conceal counterpart identity; accepted connections disclose only to participants. Acceptance and completion update the durable request assignment/handoff ledgers, and offer/connection transitions atomically create deduplicated notification intent. | Offers, connections, private transition events, notification intent, expiry, and request lifecycle linkage persist in PostgreSQL. An hourly non-overlapping sweep expires stale offers and connections. | HTTP identity tests, PostgreSQL restart and transition tests, notification source-matrix tests, and a two-context Chromium journey cover offer, decline, re-offer, accept, identity disclosure, completion, expiry rendering, block recheck, notification deduplication, export, and deactivation. | Protected staging repetition and production operations remain incomplete. |
| Inbox | `partially-persistent` | Authenticated `/inbox` and `/inbox/read` routes provide per-user activity cards for requests, offers, assignments, verification, moderation, expiry, organization notifications, and outcomes. The separate authenticated notification center provides active/read/unread/archived filters, unread counts, related-action links, verified-email setup, and explicit push opt-in/revocation. Neither workspace exposes chat, message, or conversation categories. | Activity items and notification intents have separate deduplication keys, read state, bounded metadata, occurrence time, and one-year retention in PostgreSQL. | PostgreSQL tests prove restart/read-state behavior, atomic notification creation, and aggregation without private message payloads; authenticated Chromium journeys exercise activity cards and the notification center. | Protected staging and production-provider repetition remain incomplete. |
| Notifications | `partially-persistent` | Production API startup constructs a PostgreSQL-backed durable notification service. Domain triggers atomically enqueue versioned, audience-bound intent for offer, connection, lifecycle, verification, appeal, expiry, moderation, attachment, and organization events. Authenticated routes provide list/filter/unread/read/archive and channel controls. A provider-neutral HTTPS email adapter and standards-based VAPID Web Push adapter deliver only bounded title/body/action payloads; push permission is requested only after explicit user action. Provider feedback uses a dedicated constant-time bearer boundary. | Intent, recipient, template version, deduplication key, read/archive state, verified email endpoint, push subscription, attempts, locks, exponential retry, provider idempotency, feedback, invalid-target cleanup, dead-letter state, and retention persist in PostgreSQL. Push endpoints/keys and verification tokens are excluded from export; all notification state is removed on deactivation. Exact location, private evidence, report details, contact data, and moderation notes are rejected from notification metadata and omitted from external payloads. | Clean migration replay, PostgreSQL source-matrix/restart/retry/dead-letter tests, provider-adapter tests, authenticated HTTP identity tests, export/deactivation tests, API-client tests, configuration/Compose tests, and Chromium opt-in/read/filter/revoke tests pass. Prometheus exposes pending, retry, dead-letter, stale-delivery, and sweep-failure signals with alert rules. | No protected-staging or live production-provider delivery, bounce-webhook exercise, credential rotation, or human alert-response evidence exists yet. This prevents an `externally-integrated` classification but does not leave a fixture runtime path. |
| Accepted-connection scheduling | `partially-persistent` | Authenticated, CSRF-protected, idempotent routes let either active connection participant propose a bounded window; the recipient may accept, decline, or counter-propose, and either participant may cancel a confirmed window. Every mutation locks and rechecks the connection, request, block, deactivation, and moderation state. Canonical UTC instants retain the originating IANA timezone; offset mismatch rejects ambiguous/invalid DST input. Optimistic versions reject stale writes and participant overlap rejects conflicting confirmations. | PostgreSQL stores one versioned current window per connection, bounded events, reminder eligibility, retention, privacy-safe inbox items, and notification intent. The hourly restart-safe sweep expires proposals and makes reminders eligible. Account export includes schedules; connection/workflow deactivation cascades them safely. No calendar-provider credentials or exact locations are stored. | Fresh isolated API/moderation/indexer migration replay and PostgreSQL integration cover restart, propose/counter/accept/remind/cancel, DST mismatch, stale writes, blocks, deactivation, export, notification redaction, and cascade cleanup. Production-bundle Chromium covers authenticated UI, keyboard submission, English/Spanish, 320px reflow, 200% text, and axe. The exact immutable staging release applied migration 0023 and passed readiness plus public contract probes. | A fresh credentialed participant journey and independent privacy/accessibility/translation review remain incomplete; external calendar integration is intentionally excluded. The old volunteer-shift fixture remains unwired and must be removed after any still-useful contracts are retired. |
| Organizations and resource partners | `partially-persistent` | Production API and web routes create and discover organizations, invite real DID members with hashed one-time tokens, enforce owner/admin/steward/member capabilities, assign resource stewardship, and reconfirm it on a 90-day cycle. Reconfirmation events atomically create high-priority steward notification intent. Public pages show origin, provenance when available, and a non-endorsement label. Generated `did:org:*` identities and the process-local portal were removed. | Organizations, memberships, invitations, stewardship, audit, reconfirmation events, and their notification intent persist in PostgreSQL. Safe owner deactivation transfers ownership to the oldest active admin or removes the organization when no successor exists. | PostgreSQL restart/capability/deactivation and notification source-matrix tests plus authenticated/anonymous Chromium journeys cover create, invite/accept, role controls, stewardship, reconfirmation, notification intent, provenance, and non-endorsement. | Controlled staging/partner operations remain incomplete. |
| Feedback and outcomes | `partially-persistent` | Completed-connection participants can submit one structured outcome with a bounded rating, controlled outcome/tag vocabulary, optional bounded comment, and a safety-concern tag. Submission rechecks participant, completion, block, and deactivation boundaries. A safety concern atomically creates one high-priority moderator case and notification; the UI confirms that structured review was requested. | Outcome feedback is linked to the durable connection and request, retained for one year, included in account export, and removed on deactivation/cascade. The moderator case stores only bounded outcome/rating context, a request hash, and connection reference—never the private comment, participant DIDs, or exact location. Active casework follows the moderation retention boundary. | PostgreSQL tests cover participant authorization, pre-completion rejection, duplicate suppression, restart, export, deactivation, atomic case/notification creation, privacy-safe previews, and duplicate-case suppression; Chromium covers the completed handoff, safety flag, and moderator-review confirmation. | Analytical aggregation, pilot validation, formal policy review, protected notification delivery, and production operations remain incomplete. |
| Reputation | `fixture-runtime` | The unwired `ReputationService` remains an explicit expansion experiment and computes explainable scores only from process-local signals when instantiated directly. Production API startup does not import or expose it, and production matching records that no opaque reputation score is used. | Experimental account metadata, handoffs, and feedback signals are in memory. | Expansion prototype tests were removed; source remains linted and typechecked only. | No production route, durable trustworthy signal source, manipulation controls at system boundaries, appeal process, or pilot validation. |
| Matching | `partially-persistent` | Request owners can ask the production API for advisory volunteer and resource candidates. Eligibility uses category, approximate distance, availability, language, accessibility needs, active verification and stewardship; blocked, deactivated, unavailable, expired, moderated, quarantined, or unauthorized candidates are excluded. Results provide readable factor explanations, opaque volunteer references, one deterministic global rank, and manual-only assignment. No reputation score is computed. | Matching reads current PostgreSQL request workflows, AT-derived public projections, private owner preferences, blocks, moderation, verification, and stewardship. Suggestions are intentionally recomputed and are not authority records. | PostgreSQL boundary tests prove deterministic ordering, privacy, exclusions, verification factors, block rechecks, and no automatic assignment/reputation field; Chromium proves the owner-facing explanation and manual-only UX. | Fairness evaluation on representative pilot data, protected staging repetition, and production monitoring remain incomplete. |
| Offline synchronization/PWA | `contract-only` | Shared and web modules model queued operations, conflicts, retry states, and offline UX. | Queues are model objects; no service worker, browser persistence, or production sync protocol is present. | Expansion prototype tests removed; source is typechecked only. | No installable PWA, IndexedDB queue, background sync, authenticated replay, or conflict integration. |
| Native mobile | `contract-only` | `apps/mobile` contains TypeScript API, navigation, offline-sync, and push-notification models. | No native application storage or runtime exists. | Prototype tests removed; source is typechecked only. | No React Native/Expo/iOS/Android UI, device auth, push provider, native build, or store-release pipeline. |
| Multi-region tenancy | `contract-only` | Shared multi-region contracts and API `TenantService` model routing and tenant boundaries. | Tenant state is in memory; no regional infrastructure or replicated datastore is connected. | Expansion prototype tests removed; source is typechecked only. | No deployed regions, residency enforcement, failover routing, replication, or recovery evidence. |
| External connectors | `fixture-runtime` | Connector framework, 311 adapter, and crisis-line adapter simulate initialization, records, health, and failures. | Connector configuration and records are in memory; endpoints are not exercised as real partner integrations. | Expansion prototype tests removed; source is typechecked only. | No partner credentials, durable delivery/outbox, webhook verification, real external contract tests, or operator ownership. |
| Localization | `partially-persistent` | Every production route and backend-rendered template uses key-parity English and Spanish resources with locale-aware date/number formatting. Anonymous choice persists in browser local storage; authenticated choice also persists in the durable account preference, and switching language preserves current URL/form state. | Translation resources are versioned bundle files; authenticated locale preference persists in PostgreSQL. | Thirty-five key-parity, interpolation, and raw-production-JSX checks plus production-bundle Chromium scheduling/group/chat journeys cover English/Spanish rendering and switching. The immutable staging browser probe confirmed English-to-Spanish switching and the Spanish Groups auth gate. | Professional translation review and a maintained translation-approval workflow remain external. |
| Accessibility | `externally-integrated` | Production-mode semantic components, focus helpers, announcers, explicit offline/maintenance state, reduced-motion styles, and route-level Playwright checks cover anonymous and protected shells. | Not applicable to server persistence. | The 2026-08-05 production-bundle Chromium run passed 147 runnable cases with one credentialed external journey skipped. It includes keyboard/focus/landmark checks, English/Spanish scheduling/group/chat journeys, zero-violation axe WCAG 2/2.1/2.2 scans, 320px reflow, 200% text sizing, reduced-motion enforcement, offline/stale state, origin disclosure, and public legal routes. | The linked local audit is not independent certification. No owner-supplied independent WCAG 2.2/assistive-technology review exists, so the operational `ACCESSIBILITY` NO-GO remains open. |
| Performance and capacity | `externally-integrated` | A paced real-HTTP probe exercises health, map, feed, and directory against a configured API, records completed-response latency/status/throughput, protects target URLs, and optionally runs all alpha reads concurrently. A fail-closed staging evaluator requires five minutes of mixed read, lifecycle, ingestion, moderation, resource, cleanup, and recovery evidence. | Results are emitted as redacted aggregate JSON and are not a service datastore. The evaluator schema excludes identities, credentials, exact locations, case details, and connection strings. | Probe tests cover pacing, status/error accounting, target safety, and bounded execution. Capacity-evidence tests reject short/read-only, insufficient-throughput, incomplete-cleanup, slow-projection, unsafe-resource, restarted, or unready results. An earlier five-minute immutable NUC run completed 12,000 requests at 10 RPS on each of four routes with zero errors and 85.574 ms worst p95 while three real lifecycle journeys and three moderation items completed. The 2026-08-07 40-RPS re-certification failed during measured severe unrelated host contention; a separate five-minute 8-RPS stability run completed 2,400 requests with zero errors and 213.556 ms worst p95 while all services stayed healthy with zero restarts. | The historical safe home-staging point is 40 aggregate read RPS, not a maximum, but it is not freshly re-certified for the current release. A clean-window mixed rerun, higher modeled targets, saturation, multiple replicas, independent database capacity, regional behavior, and production sizing remain unproven. |
| Deployment and operations | `externally-integrated` | Production/staging topologies provide durable migration/readiness ordering, explicit metric environments, digest-selectable images, and fail-closed secrets. Four Node 22/nginx runtime targets are built once, scanned before push, signed, accompanied by SPDX SBOM and SLSA provenance attestations, assembled into a four-image manifest, verified on the deploy host before effects, deployed with `--no-build`, and accepted by migrations/readiness/real-browser gates. Backups derive the target from the running staging API, require a matching `pg_dump`/server major, use a restrictive umask, validate and checksum an archive before atomic publication, and can replicate database and private-object snapshots to an encrypted independent S3 destination with readback verification and freshness/failure metrics; restores require an empty target, invalidate restored sessions, verify operator invariants, and measure RTO/RPO. Twenty owner-labelled Prometheus rules are loaded on the NUC. Patrick Fanella is the named interim home-staging primary on-call and escalation owner. | PostgreSQL persists API private state, projections/checkpoints, and moderation state. NUC release state retains checksummed current/previous digest manifests; deployment rejects untrusted or mutable images and rollback refuses a missing or tampered checksum. The loopback registry retains signed image and rollback digests. Backup archives carry checksum and metadata sidecars, while restored OAuth and browser sessions are deliberately discarded. | All four deployed `768f9ca5` images parse to zero HIGH/CRITICAL Trivy findings and verify against the retained local Cosign public key with SPDX and SLSA attestations. Their exact digests replayed 25/6/5 migrations idempotently, passed deep readiness and PMTiles probes, and completed a checksummed rollback to `d791fb01` followed by successful forward promotion. PostgreSQL 17 backup `patchwork_20260807_083627.dump` restored into an isolated empty PostgreSQL 17 runtime in 3 seconds with scheduling/group/chat tables present and zero browser sessions. Independent-replication tests cover encrypted transfer, object readback, byte manifests, failure preservation, and alert metrics. Earlier evidence covers metrics-series validation, real two-account OAuth/PDS journeys under mixed load, bounded capacity, and an indexer-disconnect alert/recovery drill. | The protected GHCR/OIDC workflow has not run; local-key verification deliberately uses a private-registry/no-transparency-log boundary. No credentialed independent S3 destination was available for a live replication exercise. Human alert acknowledgment, a distinct secondary responder, sustained rotation coverage, and production sizing remain unproven. |

## Confirmed reusable assets

- The five lexicons, their validators, and valid/invalid fixtures.
- Deterministic normalization, tombstone, discovery, ranking, privacy-redaction, and authorization domain logic.
- The PostgreSQL discovery-event slice and migration harness as starting patterns, not a complete schema.
- React design-system primitives, route shell, settings and discovery UX, accessibility helpers, and localization framework.
- The unit and contract test corpus as an executable specification, provided test names are not treated as proof of external integration.
- Docker build stages, configuration validation, metrics formats, and operational runbooks as scaffolding to be verified against a real staging system.

## Data-loss boundaries

A process restart preserves OAuth/session state, consent and preferences, lifecycle and safety state, moderation, idempotency, aid/directory/volunteer projections, private volunteer fields, organizations, memberships, invitations, stewardship, verification applications, private attachment metadata and object references, scan attempts, deletion jobs, evidence decisions, appeals, exact-address approvals, offers, connections, coordination windows/events/reminder eligibility, groups/memberships/rooms/invitation hashes, bounded chat conversations/messages/receipts, activity read state, notification intent/read state/delivery attempts/dead letters, outcomes, audits, tombstones, and the live indexer cursor when PostgreSQL runtimes are configured. Private attachment originals and derivatives persist in the configured object store and are reconciled after restart. A restart deliberately destroys all exact-person signaling sessions, peer proofs, SDP/ICE relay data, and browser-held exact coordinates; participants must freshly consent and establish a new peer channel. The remaining fixture-only expansion subsystems—reputation, tenants, and connectors—are not restart-durable runtime features yet.

## Verification interpretation

The 2026-08-07 repository gate passes 1,017 runnable unit/service tests with
118 environment-gated skips. Fresh
PostgreSQL scheduling, group, and chat integration gates pass, as do the
indexer/moderation migrations, exact-location absence scan, and direct service
integration. The production-bundle matrix contains 148 Chromium cases; 147
passed and one live-PDS journey remains credential-gated. The retained suite
includes durable volunteer, organization, stewardship, verification, appeal,
exact-address, offers, connections, matching, inbox, notification intent and
external-delivery adapters, outcomes, private attachment
transform/scan/access/deletion, export, deactivation, the real two-context
exact-location peer exchange, showcase separation, offline/stale states,
English/Spanish production-route axe/reflow scans, durable scheduling, groups,
and server-readable bounded chat. Passing local
and controlled home-network tests still does not substitute for protected
promotion, independent durability, staffed secondary coverage, live provider
credentials, and production operations.
